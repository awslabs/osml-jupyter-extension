// Copyright Amazon.com, Inc. or its affiliates.

/**
 * Example of [Jest](https://jestjs.io/docs/getting-started) unit tests
 */

describe('osml-jupyter-extension', () => {
  it('should be tested', () => {
    expect(1 + 1).toEqual(2);
  });
});

name: release

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  jlpm:
    uses: ./.github/workflows/jlpm.yml
    secrets: inherit
  publish:
    needs: [jlpm]
    uses: ./.github/workflows/pypi.yml
    secrets: inherit